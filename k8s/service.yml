apiVersion: v1
kind: Service
metadata:
  name: smile
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "smile"
    port: 8080
  selector:
    app: smile
