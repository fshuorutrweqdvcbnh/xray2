apiVersion: v1
kind: Service
metadata:
  name: cloud
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cloud"
    port: 8080
  selector:
    app: cloud
