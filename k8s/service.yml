apiVersion: v1
kind: Service
metadata:
  name: quaily
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "quaily"
    port: 8080
  selector:
    app: quaily
